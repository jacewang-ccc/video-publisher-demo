<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <title>Vibe Coding（AI Coding）通用流程方法论（复杂项目版）</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", Arial, sans-serif; line-height: 1.55; color: #111; }
    h1 { font-size: 20pt; margin: 0 0 10pt; }
    h2 { font-size: 14pt; margin: 18pt 0 8pt; padding: 6pt 8pt; background: #f3f6ff; border-left: 4pt solid #4c6fff; }
    h3 { font-size: 12pt; margin: 14pt 0 6pt; }
    p { margin: 6pt 0; }
    ul { margin: 6pt 0 6pt 18pt; }
    li { margin: 3pt 0; }
    .meta { color: #444; font-size: 10.5pt; }
    .kbd { font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace; font-size: 10.5pt; background: #f6f6f6; padding: 1pt 3pt; border-radius: 3pt; }
    .box { background: #fafafa; border: 1pt solid #e5e5e5; padding: 10pt; border-radius: 6pt; }
    .warn { background: #fff7f0; border-left: 4pt solid #ff8a3d; padding: 8pt 10pt; }
  </style>
</head>
<body>
  <h1>Vibe Coding（AI Coding）通用流程方法论（复杂项目版）</h1>
  <p class="meta">适用对象：不懂代码、需要做复杂产品/工具的个人或小团队。目标：快速做出可用版本（MVP），并且长期可迭代、可扩展、不容易“越做越乱”。</p>

  <h2>0. 三个核心概念（先搞清楚再开工）</h2>
  <ul>
    <li><b>MVP（范围）</b>：这一版“最小可用”包含哪些能力？不包含哪些？成功标准是什么？</li>
    <li><b>Task（执行）</b>：把 MVP 拆成一条条可验收的施工单，一次只做一条，做完再做下一条。</li>
    <li><b>Spec/Rules（硬规则）</b>：把“容易忘、容易扯皮、容易反复改”的规则写死，让 AI 在多轮对话、多任务窗口下仍能做一致。</li>
  </ul>
  <p class="warn"><b>关键关系：</b>先定 MVP（做什么/不做什么）→ 再拆 Task（怎么一步步做出来）→ 规则复杂时补 Spec/Rules（保证一致可扩展）。它们不是二选一。</p>

  <h2>1. 时间顺序总览（你可以照着走的“主流程”）</h2>
  <div class="box">
    <p><b>启动（第 0 天 / 第 1 小时）</b>：GOAL → AI_CONTEXT</p>
    <p><b>需求成形（第 0–2 天）</b>：PRD（粗→细）→（必要时补）FLOWS/WIREFRAMES</p>
    <p><b>实现 MVP（第 1–7 天）</b>：BACKLOG/优先级 → 拆 tasks → 一次做一个 task → 验收</p>
    <p><b>稳定与扩展（贯穿整个周期，按触发条件开启）</b>：DECISIONS → SPEC/RULES → DoD 统一口径 → 变更管理</p>
  </div>

  <h2>2. 启动阶段（必须做，10–40 分钟）</h2>
  <h3>2.1 写清楚“做什么/不做什么”（必做）</h3>
  <p>产物：<span class="kbd">docs/GOAL.md</span></p>
  <ul>
    <li>目标：一句话（例如“让用户用一个面板准备并确认发布到多个平台”）。</li>
    <li>成功标准：可观察（例如“把原来 4 次重复发布减少为 1 次准备 + 1 次确认”）。</li>
    <li>不做什么：列 5 条（例如“暂不做自动发布绕过验证码”“暂不做复杂数据报表”）。</li>
  </ul>

  <h3>2.2 建立“项目记忆”（必做）</h3>
  <p>产物：<span class="kbd">docs/AI_CONTEXT.md</span>（建议 1–2 页）</p>
  <ul>
    <li>项目一句话与当前 MVP 范围。</li>
    <li>术语表（你们对关键名词的定义）。</li>
    <li>当前进度（此时可以是“未开始/准备中”）。</li>
    <li>怎么运行/怎么验收（先留空也行，后面补）。</li>
  </ul>

  <h2>3. 需求成形（先粗后细：先做出来，再逐步补全）</h2>
  <h3>3.1 PRD（建议必做；先粗后细）</h3>
  <p>产物：<span class="kbd">docs/PRD.md</span></p>
  <p><b>PRD 最小结构（推荐）</b></p>
  <ul>
    <li>用户是谁 &amp; 典型使用场景（1–3 个）。</li>
    <li>端到端用户流程（从“打开”到“完成”的步骤）。</li>
    <li>页面/模块清单（有哪些区块，分别干什么）。</li>
    <li>字段与规则（默认值、可选项、约束、例子）。</li>
    <li>MVP 范围（v0.1 做什么/不做什么）+ 版本规划（v0.2/v0.3）。</li>
    <li>版本级验收标准（5–10 条）+ 风险与限制。</li>
  </ul>

  <h3>3.2 流程图/信息架构/线框（可选，但复杂交互强烈建议）</h3>
  <p>产物：<span class="kbd">docs/FLOWS.md</span>、<span class="kbd">docs/WIREFRAMES/</span></p>
  <p><b>两种工作模式（都正确）</b></p>
  <ul>
    <li><b>探索型（先做再改）</b>：先做一个“能跑能点”的粗 UI，再补流程/线框来减少返工。</li>
    <li><b>结构型（先设计再做）</b>：先画线框+状态切换（编辑/保存/预览/禁用等），再让 AI 按图实现。</li>
  </ul>
  <p><b>触发条件（出现任意两条就建议补流程/线框）</b></p>
  <ul>
    <li>同一个页面返工 ≥ 2 次。</li>
    <li>你说不清“哪些模块、哪些状态、先后顺序”。</li>
    <li>交互存在“编辑中/已保存/预览/草稿/锁定”等多状态。</li>
  </ul>

  <h2>4. 任务拆分与执行（MVP 主战场）</h2>
  <h3>4.1 先做 Backlog 与优先级（强烈建议）</h3>
  <p>产物：<span class="kbd">docs/BACKLOG.md</span>（也可写入 AI_CONTEXT 的一节）</p>
  <ul>
    <li>把需求拆成“功能清单”，标记优先级：P0（必须）/P1（重要）/P2（可选）。</li>
    <li>每条功能写 1 句“价值描述”和 1 句“验收口径”。</li>
  </ul>

  <h3>4.2 拆 Task（必须）</h3>
  <p>产物：<span class="kbd">tasks/task_XXX.md</span></p>
  <p><b>Task 粒度标准</b></p>
  <ul>
    <li>1–3 小时内可完成。</li>
    <li>你能用 5–10 条验收标准检查完。</li>
    <li>改动集中在一个页面/一个模块/一个数据结构。</li>
  </ul>
  <p><b>Task 模板（建议每个 task 都按这个写）</b></p>
  <ul>
    <li>目标（做成什么样）。</li>
    <li>不做什么（本任务明确不包含的范围）。</li>
    <li>验收标准（5–10 条，可操作、可判断对错）。</li>
    <li>手动测试步骤（你点哪里、看什么结果）。</li>
    <li>回滚方式（不满意怎么撤：关开关/恢复文件/撤销某配置）。</li>
  </ul>

  <h3>4.3 执行顺序（必须严格遵守）</h3>
  <ul>
    <li>一次只做一个 task。</li>
    <li>AI 实现 →（尽量）自测 → 你按验收标准验收 → 通过再进入下一 task。</li>
    <li>每个 task 完成后：把关键变化写回 AI_CONTEXT（进度/入口/验证方式）。</li>
  </ul>

  <h2>5. 稳定层：什么时候开始做？做什么？</h2>
  <p>稳定层不是“最后才做的一层”，而是<b>按触发条件开启的伴随式工作</b>。它的目标是：避免反复解释、避免跑偏、避免换窗口断片、保证规则一致。</p>

  <h3>5.1 DECISIONS（建议从第一个关键选择就开始）</h3>
  <p>产物：<span class="kbd">docs/DECISIONS.md</span></p>
  <p><b>何时写：</b>每出现一个“会影响后续很多工作”的选择就写一条（例如技术路线、交互原则、扩展策略）。</p>
  <p><b>每条格式：</b>决策是什么 / 为什么 / 备选方案 / 影响与后果。</p>

  <h3>5.2 SPEC/RULES（规则复杂时再加，但别等太晚）</h3>
  <p>产物：<span class="kbd">docs/SPEC.md</span> 或 <span class="kbd">docs/RULES.md</span></p>
  <p><b>触发条件（任意两条就建议建立最小 SPEC）</b></p>
  <ul>
    <li>出现第二处以上“优先级/覆盖/默认值冲突”。</li>
    <li>开始做可配置（模板/流程/字段映射会变）。</li>
    <li>开始多平台差异（同一概念不同平台不同字段/约束）。</li>
    <li>你发现同样的规则要反复讲给 AI。</li>
  </ul>
  <p><b>最小 SPEC 应包含（不需要写技术实现）</b></p>
  <ul>
    <li>术语与数据对象：有哪些配置/字段/状态。</li>
    <li>默认值、优先级、覆盖关系（谁覆盖谁，何时生效）。</li>
    <li>错误处理：缺失必填/保存失败/生成失败时怎么提示。</li>
    <li>验收口径：可测试条目。</li>
  </ul>

  <h3>5.3 DoD（完成定义，统一口径）</h3>
  <p>产物：写在 <span class="kbd">tasks/task_000_template.md</span> 顶部或 <span class="kbd">docs/SPEC.md</span></p>
  <p><b>建议的 DoD（按需选用）</b></p>
  <ul>
    <li>关键路径可用（能跑、能点到目标结果）。</li>
    <li>手动测试步骤全部通过。</li>
    <li>刷新/重开后关键配置不丢（如果本 task 涉及保存）。</li>
    <li>错误提示清楚（如果本 task 涉及必填/失败）。</li>
    <li>更新 AI_CONTEXT：进度/入口/下一步。</li>
  </ul>

  <h3>5.4 变更管理（避免“边做边改导致全乱”）</h3>
  <p>产物：写入 <span class="kbd">docs/DECISIONS.md</span> + <span class="kbd">docs/BACKLOG.md</span> + 更新对应 task</p>
  <p><b>规则：</b>需求临时改动时，至少做三件事：</p>
  <ul>
    <li>记录：为什么改（DECISIONS）。</li>
    <li>调整：优先级/范围（BACKLOG）。</li>
    <li>更新：相关 task 的验收标准（tasks）。</li>
  </ul>

  <h2>6. 验收标准怎么写（你不懂代码也能写的模板）</h2>
  <p><b>验收标准必须满足：</b>可观察、可操作、可判断对错。</p>
  <p><b>四类常用验收点</b></p>
  <ul>
    <li>界面类：出现/隐藏某模块；点击后状态变化正确。</li>
    <li>数据类：保存后不丢；刷新后仍存在；新增项可被选到。</li>
    <li>规则类：选择 A 生成 B；选择 C 生成 D；优先级与覆盖正确。</li>
    <li>异常类：缺失必填提示明确；失败不会清空旧数据；有可恢复路径。</li>
  </ul>

  <h2>7. 统一上下文（每个 Task 都必须“先读懂”的东西）</h2>
  <p>建议固定为 4 份“项目记忆包”，每次开新任务/新窗口都先读取：</p>
  <ul>
    <li><span class="kbd">docs/AI_CONTEXT.md</span>：项目事实单（1–2 页，短、权威）。</li>
    <li><span class="kbd">docs/DECISIONS.md</span>：关键决策（防走回头路）。</li>
    <li><span class="kbd">docs/SPEC.md</span>（若存在）：硬规则合同（默认值/优先级/覆盖/异常）。</li>
    <li><span class="kbd">tasks/task_XXX.md</span>：当前施工单（目标/验收/测试/回滚）。</li>
  </ul>

  <h2>8. 任务窗口上下文快满/满了怎么办（标准 SOP）</h2>
  <p><b>口诀：</b>先落盘，再换窗。</p>
  <p><b>步骤（固定顺序）</b></p>
  <ol>
    <li>把本轮新增共识写入：规则→SPEC/RULES；决策→DECISIONS；进度→AI_CONTEXT。</li>
    <li>在当前 <span class="kbd">tasks/task_XXX.md</span> 末尾记录：做到哪、卡点、下一步 3 条。</li>
    <li>开新窗口固定开场白：请先阅读 AI_CONTEXT、DECISIONS、SPEC（若有）、当前 task，然后按验收标准继续。</li>
  </ol>

  <h2>9. 最小可执行清单（你每次项目都可复用）</h2>
  <ul>
    <li>必做：GOAL → AI_CONTEXT → PRD（粗）→ BACKLOG(P0/P1/P2) → tasks → 一次做一个 task。</li>
    <li>触发补充：返工/状态复杂 → FLOWS/WIREFRAMES；规则复杂/可配置/多平台 → SPEC/RULES。</li>
    <li>贯穿始终：DECISIONS（关键选择就记）+ 每 task 更新 AI_CONTEXT（防断片）。</li>
  </ul>

  <p class="meta">版本：v1.0（通用流程稿）。你可以根据项目复杂度删减文档，但建议保留“AI_CONTEXT + tasks 验收标准 + 关键决策记录”。</p>
</body>
</html>

